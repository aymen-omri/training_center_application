import { FormerListPipe } from './former-list.pipe';

describe('FormerListPipe', () => {
  it('create an instance', () => {
    const pipe = new FormerListPipe();
    expect(pipe).toBeTruthy();
  });
});
